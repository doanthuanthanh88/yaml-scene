# - Mongo:
#     connection: mongodb://root:example@localhost/test
#     queries:
#       - db.col('User').find()

- ~/code/github/runow-extension/mongo#Mongo:
    connection: mongodb://root:example@localhost/test
    queries:
      - db.col('User').find()
