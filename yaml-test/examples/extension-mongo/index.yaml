# - Mongo:
#     connection: mongodb://root:example@localhost/test
#     queries:
#       - db.col('User').find()

- ~/code/github/yamlex-extension/mongo#Mongo:
    connection: mongodb://root:example@localhost/test
    logLevel: debug
    title: Test mongo local
    queries:
      - title: Get list users
        query: db.col('User').find()
        var: rs
- Echo: ${rs}
